# 原版对照表（§2 必交付物）

> **规则**：每行 = 一个元素 —— `元素 | 原版值（含出处） | 我们的值 | 差值`。
> **差值为 0 才算过**；**写不出原版出处的那一行 = 该元素未验证**。
> **表里没有的行 = 这个元素没人搬过**（不允许"实现了但没进表"）。
> 覆盖范围：几何与碰撞（含通行性）、模型与动画数据、贴图、界面元素的坐标/尺寸/颜色、数值与公式。

**参考物 A**：Super Mario Bros.（NES, 1985）World 1-1 / 1-2。
**权威载体**：`原版资源/参考工程/SMB-clone`（1-1/1-2/两个密室场景 + 全部预制体 + 玩家控制器脚本）、
`原版资源/参考工程/Unity-SuperMarioBros`（1-1 数据 + 原版图集）。
**本轮（2026-09-19 · 平台保真与物理数值）**：把第四节整节从"`未验证`"改成"运行时读值 vs clone 真值"，
并复刻升降台三项差异（E-21）。
**本轮（2026-09-19 05:1x · 火球速度收口）**：第四节新增 6 行「火球」—— 水平 20 / 初速（向下）−11 / 反弹 +11 /
重力 53.96 / 联合印证 / 出生点（未搬，登记 E-22）+ 寿命（本项目新增）。读数全部来自**新写的采样器**
`Probe.Physics` 的 `MeasureFireball`：马里奥**从半空发**火球（下落段够长才采得到初速）、速度按**整窗**算、
分段按"整窗落在哪一段"判 —— 这三条都是实测踩出来的（见 `验收表` 表体 #17）。

**本轮新增的可复跑核对器**（都在 `原版资源/解析/脚本/`，退出码 0 = 全 PASS）：
| 脚本 | 管什么 | 判据来源 |
|---|---|---|
| `check_physics.py` | 第四节（重力/跳跃/走跑速/敌速/时间） | 现解析 clone 的 `Mario.cs` / 敌人 prefab / `KoopaShell.cs` / `GameStateManager.cs` / `Physics2DSettings` / `TimeManager`，与游戏日志里 `Probe.Physics` 的**活对象读数**逐项比 |
| `check_platform.py` | 升降台（行程/速度/止点/出现点/台面尺寸/生成周期/离屏销毁） | 现解析 clone 的 spawner prefab + 场景实例 + 平台 prefab + spawner 脚本，与 `Probe.PlatformSpawn` 的读数/日志比 |
| `gen_platform_sprite.py` | 台面贴图 | 从 clone `Moving Platform Vertical.prefab` 的 `m_Sprite` → `misc-3.gif` 的切片 rect 裁出来（带 y 轴翻转自检 + 尺寸=prefab `m_Size`×16 自检） |

---

## 一、关卡几何与数据

| 元素 | 原版值（出处） | 我们的值 | 差值 |
|---|---|---|---|
| **1-1 瓦片总数·地形列** | **557**（出处：`原版资源/参考工程/SMB-clone/Assets/Scenes/World 1-1.unity` 逐 PrefabInstance 展开 + 去重，`python 原版资源/解析/脚本/solid_profile.py` 输出 `clone solid cells=601`、与我们的实心集交集 **556/557**） | **557**（出处：`client/Assets/Resources/Levels/World1-1.txt` 的 layer-0 行；运行时 `关卡已解析 Levels/World1-1：瓦片 812`） | **0**（**逐格**相同 556/557；`clone 601` 的差是把方块实例也展开成格 + 最左地面列，见下两行） |
| 1-1 装饰（山丘 / 云 / 灌木） | **原版整关海报逐格解出的 255 格**（出处：`策划/基线图/nes-original-1-1-full-map.png`，对齐 `col=x+14 / row=9-y`；实例几何出处 = clone `World 1-1.unity` 的 **42 个** `Prefabs/Scenery/*` 实例 + `Assets/Sprites/*.meta` 的 spriteSheet 矩形） | `World1-1.txt` 的 **255 行** `SceneryTileSprites_*`（由 `原版资源/解析/脚本/gen_scenery_11.py` 生成，可复跑） | **0** —— 240 格**逐格等于海报**（类别图与调色板无关；`gen_scenery_11.py --check` 7 项全 PASS）；另 15 格海报上被**前景精灵**（马里奥 ×1 / 栗宝宝 ×5 / 乌龟 ×1）或**前景地形**（大理石台阶 ×5 / 城堡 ×2）遮住 ⇒ 不可判定，按同 prefab 戳记补上并**逐格登记** |
| 1-1 实心格数 | **601**（clone 展开；出处同"瓦片总数·地形列"） | **557** | **+44（已解释、非缺陷）**：clone 的展开把 **43 个方块实例**当成实心格（我们记成**实体**：`E` 行共 44 个方块，其中隐形 1-UP 那块没贴图、不参与实心展开），**地面另差 2 格** —— ⚠️ 那 2 格在 clone 的**右端** `(210,-5)/(210,-4)`（**不是**最左列；本表旧措辞"最左地面列 x=−14"是笔误，2026-09-19 逐格复核后订正）。逐格层面：我们 557 格中 **556 格**在 clone 里存在；唯一不在 clone 里的那 1 格是 `(184,-3)`（旗杆基座 —— clone 那边是 `Goal Post` prefab，不属于 `Prefabs/Ground`、`Prefabs/Blocks`、`Prefabs/Platforms`、`Prefabs/Pipes` 这四个目录的实心展开） |
| **1-1 地面连通段两端**（2026-09-19 逐格复核，三个载体同看） | clone：`[(-14,54),(57,71),(75,138),(141,210)]`；原版整关海报（映射后）：`[(-14,54),(57,71),(75,138),(141,209)]`；tilemap（`Level1.unity`，= txt 地形行的直接来源）：`[(-13,54),(57,71),(75,138),(141,209)]` | `World1-1.txt`：`[(-14,54),(57,71),(75,138),(141,209)]` | **左端差 0**：`(-14,-5)/(-14,-4)` 在位 —— 与海报 col 0、clone 最左地面块（覆盖 x=-14..-8）都一致；旧登记项「最左地面列缺 2 格」**已过期**（当时 txt 起点是 -13，那 2 格此后已补）。**右端差 1 列 / 2 格**：clone 的 7 格宽地面块外溢到 `(210,-5)/(210,-4)`，而海报右边界 = x=209（截在图边、无法仲裁）、tilemap 也止于 209 ⇒ **不补**（补 = 往原版没有的地方加瓦片）。复跑判据 = `python 原版资源/解析/脚本/gen_11_level.py --check` 的 ①a~①f |
| 1-1 栗宝宝数 | **16**（出处：clone `World 1-1.unity` 的 16 个 `Prefabs/Enemies/Brown Goomba*` 实例，逐实例行号见 `原版资源/解析/原版1-1与1-2元素表.md` §1.5） | **16**（`World1-1.txt` 的 16 行 `E … Goomba`；运行时 `实体已铺开：栗宝宝 16`） | **0** |
| 1-1 砖块数 | **28**（出处：clone 的 28 个 `Brown Brick Block- Regular*` 实例，元素表 §1.2） | **28**（`E … Brick` 28 行） | **0** |
| 1-1 问号块数 | **13**（出处：clone 的 `Question Block- Coin*` / `Brown Question Block- Coin*`（10 个）+ `Question Block- Powerup*`（3 个），元素表 §1.2） | **13**（`E … MysteryBox` 10 行 + `E … MysteryBoxMushroom` 3 行） | **0** |
| 1-1 道具块数（含蘑菇/火花的问号块） | **3**（出处：clone 的 3 个 `Question Block- Powerup*`，元素表 §1.2 第 3/10/29 行） | **3**（`E … MysteryBoxMushroom`：`(7,0)` `(64,0)` `(95,4)`） | **0** |
| 1-1 乌龟数 | **1**（出处：clone `World 1-1.unity:3671` 的 `Green Koopa`，元素表 §B1-1） | **1**（`E 93.5 -2.5 Koopa`） | **0**（元素表 §B1-1 登记的"缺乌龟"已消除） |
| 1-1 **隐藏 1-UP 砖块** | `Scenes/World 1-1.unity:617`（GO `Hidden Question Block- Oneup`）+ `:4930`（`m_LocalPosition {x:59,y:4}`）⇒ 本工程格 **(50,1)** | 已落地：`T(50,1)` 隐形 + 实心，顶到出 1-UP（验收表 #47） | **0**（原登记项 E-1 已消除） |
| 1-1 **金币房进水管** | `Scenes/World 1-1.unity:3778`（`Warp Green Pipe 2x4 Down` @ clone (53,0)）⇒ 本工程格 x=43..44 | 已落地（`World1-1.txt` + `PipeWarpTable.Level11`；验收表 #50） | **0**（原 E-1 已消除） |
| 1-1 无敌星 | `Scenes/World 1-1.unity:4633`（`Brown Brick Block- Starman` @ clone (96,3)）⇒ 本工程格 **(87,0)** | 已落地：`T(87,0)` 是星砖（验收表 #49） | **0**（原 E-1 已消除） |
| 1-2 地下密室 | `SMB-clone/Assets/Scenes/World 1-2 - Underground.unity`（元素表 §4.2，17 枚币） | 已落地（`World1-2-Underground.txt`；验收表 12-10） | **0** |
| **1-2 地下段结尾**（墙 / 侧向管口 / 管身 / 横梁 / 地板缺口） | clone `World 1-2.unity`（元素表 §4.1；`check_sections_12.py` 第 3 段）；**侧向管的落格另有权威 = 原版整关海报** `策划/基线图/nes-original-1-2-full-map.png`（原版游戏的像素、整数格；定标 `col=x+3 / row=27−y` 由地板缺口与 y=10 横梁两条独立特征各自验证） | `World1-2.txt` x≥145：墙 115 格实心、**侧向管 20 格（管口 2x2 = x=163,164 / y=3,4；拐角+管身 = x=165,166 / y=3..10）全 layer=0（实心，与原版 prefab 的实体碰撞体一致）**、横梁 y=10 x=158..180、地板缺口 x=152..159 | **0**（与 clone 逐格一致；侧向管与**原版海报**逐格一致 —— `python 已清理（一次性产物）`：管格集合逐个相等 + 窗口 135 格类别一致，summary PASS） |
| **1-2 地表段**（出管 / 地面 / 大理石台阶 / 旗杆 / 城堡 / 食人花） | clone `World 1-2 - Castle Cut.unity`（元素表 §4.4；`check_sections_12.py` 第 2 段） | `World1-2-Surface.txt`：地面 x=0..41、出管 4 格、大理石 44 格、`# flagpole 19 0`、`# castle 25 2`、**`# spawn 0.5 1.5`（= clone `Spawn Point` 真值）+ `# pipe-rise 0.5 2`（管顶站姿）**、食人花 (0.5,0) | **0**（地形/道具）；装饰 39 格见 E-14 消除记录；**出生点与出管升起过场 2026-09-19 接上**（见 E-15 消除记录：运行时 `出管升起：起点=(0.50,1.50) 终点=(0.50,2.00)`） |
| 1-2 瓦片总数 | 808（出处：clone `World 1-2.unity` 解析；按 prefab 默认尺寸**粗展开**） | **823**（2026-09-19 18:04 运行时实测 `关卡已解析 Levels/World1-2：瓦片 823、实体 51`；结尾区 x≥145 按 clone 真值重写、侧向管挪到**海报落格**（x=163..166）后由旧读数 819 变 823） | 差 **+15（已解释、不是缺陷）**：clone 的 808 是"按 prefab 默认尺寸展开"的**粗口径**（既不含侧向管那 20 格、也不含结尾区补回的墙砖），我们的 823 是**逐格真值**。权威判据是**逐格**比对而不是总数：`python 原版资源/解析/脚本/check_sections_12.py` → **FAIL=0**、`python 已清理（一次性产物）` → **826 == 826、缺 0 多 0** |
| 1-2 实体数 | 51（同上：Coin 17 / Goomba 14 / MysteryBox 7 / Koopa 4 / MushroomBox 3 / Piranha 3 / MovingPlatform 2 / OneUpBox 1） | 51（`实体已铺开：栗宝宝 14、乌龟 4、食人花 3、金币 17、问号块 7、道具块 3、1-UP 块 1、移动平台 2`） | 0 |
| 1-2 地表段瓦片 / 实体 | 29 个对象（21 prefab + 8 容器，元素表 §4.4） | `Levels/World1-2-Surface` 运行时日志：**瓦片 171、实体 1（食人花）、范围 x[0,41] y[-2,10]** | 0（地形/道具）；装饰见 12-14 |

## 二、贴图与素材

| 元素 | 原版值（出处） | 我们的值 | 差值 |
|---|---|---|---|
| 瓦片图集 | `原版资源/参考工程/Unity-SuperMarioBros/Assets/Sprites/WorldTileSprites.png` + `SceneryTileSprites.png` | `client/Assets/Resources/Sprites/Tiles/**`（同源复制） | 0（同源） |
| 马里奥图集 | 同上 `smb_mario_sheet.png` | `client/Assets/Resources/Sprites/Mario/**` | 0（同源） |
| 敌人工集 | 同上 `smb_enemies_sheet.png` | `client/Assets/Resources/Sprites/Enemies/**` | 0（同源） |
| 道具图集 | 同上 `smb_items_sheet.png` | `client/Assets/Resources/Sprites/Items/**` | 0（同源） |
| 旗杆 / 城堡 / 砖块碎片 | 同上 `FlagpoleSprites.png` / `Castle.png` / `BrickParticleSprites.png` | `client/Assets/Resources/Sprites/{Flagpole,Castle,Particles}/**` | 0（同源） |
| **升降台台面贴图**（**本轮新增**） | `SMB-clone/Assets/Prefabs/Platforms/Moving Platform Vertical.prefab:222` 的 `m_Sprite`（`fileID: 21300014`）⇒ `Assets/Sprites/misc-3.gif` 的切片 `moving_platform_6`，`.meta` rect = **x=143, y=1201, 48×8 px**（= 3×0.5 格，与同 prefab `:121` 的 `m_Size {x:3,y:0.5}` 一致） | `client/Assets/Resources/Sprites/Platform/MovingPlatform.png`（48×8 px，`ResPaths.PlatformDir`；裁切脚本 `gen_platform_sprite.py` 可复跑 + 自检） | **0**（同源裁切；脚本自检 5 项全 PASS —— 含"y 轴翻对/翻反"的正反自检） |
| **侧向管贴图**（管口 / 拐角 / 管身；= 登记项 **E-8，已修**） | `SMB-clone/Assets/Sprites/misc-3.gif` 的 `pipe_green_top_side`（`.meta` rect = **x=573, y=1118, 32×32**）/ `pipe_green_mid`（rect = **x=610, y=1118, 34×32**）/ `pipe_green_body`（rect = **x=616, y=1154, 28×16**） | 管口 2x2 = `WorldTileSprites_31`+`_44`（左列 上/下）+`_32`+`_45`（右列 上/下）；拐角 2x2 = `_57`+`_70`（左列）+`_58`+`_71`（右列）；管身 = `_18`/`_19` | **0**（`check_sections_12.py` 第 1 段：四组都 **差 0/512**、管身 **差 0/448**；旧版是立管管身 `_18/_19` 当管口，最小差 146/512）<br>⚠️ 2026-09-19 订正：旧表写的 rect `(573,102) 16x32` / `(589,102) 16x32` 与 "`_32`/`_45` = 中段" **都是错的**（那两个 rect 是假的搜索窗；`_32`/`_45` 其实是 `top_side` 的右半）—— 见 `check_sections_12.out.txt` 第 4~13 行 |
| **管口/拐角 8 张瓦片 vs 原版整关海报（像素残差）** | `SMB-clone/Assets/Sprites/misc-3.gif`（**游戏数据**）的 `pipe_green_top_side` / `pipe_green_mid`；另有一份 **参考图**级载体 = 原版整关海报 `策划/基线图/nes-original-1-2-full-map.png`（重绘、另一套调色板） | 那 8 张 = clone `misc-3.gif` 的**逐像素**复制（`check_sections_12.py` 第 1 段：四组 **差 0/512**、管身 **差 0/448**） | **0（对权威载体 clone）**；与**海报**的 `48~138/256` 残差 **= 已解释、不是缺陷**：① §7 层级 **clone 的游戏数据 > 海报参考图**（海报是另一种绘制/调色板）；② 同一次比对上连"管身 / 砖"（同一根管、同一批图）也只有 `16/256` ⇒ 残差随**载体**变、不随**格**变；③ `python 已清理（一次性产物）` 用**与调色板无关的 3 值类别图**判 **PASS**（管格 20 个逐个相等 + 窗口 135 格类别逐格一致）⇒ 位置与形状与原版一致。⛔ **不照海报改瓦片**（改了就是拿参考图覆盖游戏数据） |
| **像素基准** | 16 px = 1 瓦片（SMB 原生） | `spritePixelsPerUnit = 16`（`tools/ai-skill/conventions.md`） | 0 |

## 三、界面（HUD / 菜单）像素坐标

| 元素 | 原版值（出处） | 我们的值 | 差值 |
|---|---|---|---|
| **HUD 四栏（列位置）** | 四栏在**原版首屏**上的墨迹列区间（出处：`策划/基线图/nes-original-1-1-first-screen.png`，256×240，16 px = 1 格；量法 = HUD 带内按白墨迹聚类）：`MARIO/000000` x=24..70 ｜ `×00` x=97..118 ｜ `WORLD/1-1` x=144..182 ｜ `TIME/400` x=201..230 ⇒ **归一化左/中/右 = 0.0938 / 0.3789 / 0.5625–0.7109（中 0.6367） / 0.7852–0.8984** | `HudPanel.cs` 四栏锚点 = **0.09375 / 0.34766 / 0.63672（居中）/ 0.91016（右对齐）**（= 原版那四个区间的归一化值，用**归一化锚点**而不是像素偏移 ⇒ 换分辨率不错位） | **列锚点差 = 0（设计基准就是上列原版区间）**；⚠️ **像素级复核 = HUMAN-ONLY**：本工程画面是 16:9（1096×500 的取景能看 26 格宽），原版是 4:3（256×240 看 16 格宽）⇒ 两边**不可能**同时做到"字号占屏比同"与"列 x 分数同"；我们选了**列位置按分数对齐**，字号沿用 16px 像素字 ×1.6（原版字为 8px = 0.5 格）。已登记「允许的差异」**E-22**。自动量法见 `已清理（一次性产物，随 .ai-tmp 删除）`（白墨迹聚类；天空里的白云会污染该量测 ⇒ 该项不自动判 PASS） |
| 标题屏元素 | logo / `©1985 NINTENDO` / `TOP- 000000`（出处：`SMB-clone/Assets/Sprites/title.png`） | 同（用同一张原版图） | 已一致（验收表自审节） |

## 四、数值与公式（**手感参数的核查入口**）

> **运行时值一律从活对象读**（探针 `Probe.Physics` 读 `IPlayer.Velocity` / 敌人 `transform` / `Score.TimeLeft`；
> `Probe.PlatformSpawn` 读台面 `SpriteRenderer.bounds`），**不从代码常量抄**。
> 复核：`python 原版资源/解析/脚本/check_physics.py`（**退出码 0 = 全 PASS**，本轮读数见下表）。

| 元素 | 原版值（出处） | 我们的值（运行时读数） | 差值 |
|---|---|---|---|
| **重力·下落** | **85.27 格/秒²**（出处：`Mario.cs:93-94` `jumpDownGravity = 1.64` × `Level Starter.prefab:871` `m_GravityScale: 5.3` × `Physics2DSettings.asset:7` `m_Gravity y=-9.81`） | **85.27**（`Probe.Physics` 采 `vy -5.50 → -19.94` 的斜率，Δt=0.169s；只在 \|vy\|<20 的区间取，避开落速上限） | **0.00** |
| **重力·上升（按住 A）** | **24.44 格/秒²**（出处：同 prefab `:90-92` `jumpUpGravity = 0.47`） | **24.44**（采 `vy 14.77→5.20`，Δt=0.391s） | **0.00** |
| **起跳初速（走/中速档）** | **15 格/秒**（出处：`Mario.cs:92` `jumpSpeedY = 15f`，档位条件 `:91` `currentSpeedX < 3.75`） | **14.77**（起跳后第一帧的 `vy` 峰值；差的 0.23 = 半帧重力，容差 ±1.5） | **-0.23**（在容差内） |
| **起跳初速（奔跑档）** | **18.75 格/秒**（出处：`Mario.cs:100` `jumpSpeedY = 18.75f`，档位条件 `:99` 速度 ≥ 8.67） | **18.48**（跑跳，起跳前 \|vx\|=9.61） | **-0.27**（同上半帧重力） |
| **跳跃参数分档** | 按起跳瞬间的水平速度三档：`|vx|<3.75` ⇒ 15/0.47/1.64；`<8.67` ⇒ 15/0.44/1.41；否则 18.75/0.59/2.11（出处：`Mario.cs:91-103` `SetJumpParams()`） | `GameConst.JumpParamsFor(speedX)` 逐条搬运同一张表（`PlayerActor.JumpAndGravity` 用当前 \|vx\| 取档） | **0**（表逐项相同；跑跳实测 18.48 ⇒ 确实进了第三档） |
| **满按跳峰值高度** | **4.60 格**（= 15²/(2×24.44)，由上面两个原版值推出） | **4.575 格**（采样 93 点，起跳点 y=−3） | **-0.03** |
| 可变跳跃高度（松手早 ⇒ 跳得低） | 原版靠**变重力**（上升按住用小重力，松手切大重力；`Mario.cs:206-212`），**没有"截断速度"那套** | 短按 0.06 秒 ⇒ 峰值 **1.934 格**（满按 4.575 格）；代码已删掉本项目自加的 `JumpCutVelocity` | **0**（机制与原版同：只有"变重力"、没有"截断速度"；那一行里的 2.64 格 = 本工程**短按 vs 满按**的自身对比，**不是**与原版的差值） |
| **走速上限** | **5.86 格/秒**（出处：`Mario.cs:36` `maxWalkSpeedX = 5.86f`，直接写进 `rigidbody.velocity`） | **5.860**（按住 → 稳态 \|vx\|，0.82 秒到顶） | **0.000** |
| **跑速上限** | **9.61 格/秒**（出处：`Mario.cs:37` `maxRunSpeedX = 9.61f`，按住 Shift） | **9.610**（1.23 秒到顶） | **0.000** |
| 地面加速度 | 走速以下 7.0 格/秒²、已到走速且按住 Shift 10.5 格/秒²（出处：`Mario.cs:31-32` `walkAccelerationX .14` / `runAccelerationX .21` ÷ `TimeManager.asset` `Fixed Timestep 0.02`） | `GameConst.GroundAccelWalk/GroundAccelRun` = 7.0 / 10.5（走速 0.82s、跑速 1.23s 到顶 ⇒ 与两档加速度自洽） | **0**（间接：由到达上限的用时印证） |
| 松手减速度 / 滑铲减速度 / 滑铲阈值 | 12.5 / 25.0 格/秒² / 3.5 格/秒（出处：`Mario.cs:33-35` `.25` `.5` `skidTurnaroundSpeedX 3.5`） | `GameConst.GroundFriction` 12.5、`SkidDecel` 25.0、`SkidThreshold` 3.5 | **0** |
| 空中加速 / 反向减速 | 空中：速度 <5.86 ⇒ 7.0，否则 10.5；反向减速三档 7.0/9.5/10.5（出处：`Mario.cs:106-118` `SetMidairParams()`） | `GameConst.AirAccelWalk/AirAccelRun/AirTurnDecel` 同值 | **0** |
| 最大下落速度 | **原版无此上限**（clone `Mario.cs` 里没有） | **24 格/秒** —— **本项目新增**：逐格解算碰撞要求"单帧位移 < 半格"（24 @60fps = 0.4 格）；已登记「允许的差异」**E-22** | 差异已登记（不是原版值） |
| **1-1 初始时间** | **400**（出处：`GameStateManager.cs:46` `ConfigNewGame(): timeLeft = 400.5f` ⇒ HUD `RoundToInt` 显示 400；时间流速出处 `LevelManager.cs:127` `timeLeft -= Time.deltaTime / .4f` 注释 `1 game sec ~ 0.4 real time sec`） | **400**（进关后探针首次读到 **399** —— 已过 1 个 tick；进关/重开走同一个 `Score.ResetForLevel()` ⇒ 用死亡重开后读到的 **400** 作为该常量的运行时证据） | **0** |
| **死亡后重开时间** | **400**（出处：`GameStateManager.cs:59-62` `ConfigReplayedLevel(): timeLeft = 400.5f`） | **400**（`Probe.Physics`：掉出关卡 → 重开 → 读到 `TimeLeft = 400`） | **0** |
| 时间流逝速率 | 每 **0.4 秒**掉 1（出处：`LevelManager.cs:127`） | `GameConst.TimeTickInterval = 0.4f` | **0** |
| **敌人速度·栗宝宝** | **2.5 格/秒**（出处：`Prefabs/Enemies/Brown Goomba.prefab:139` `Speed: {x: 2.5, y: 0}`，由 `_common/MoveAndFlip.cs:51` 写进 `velocity`） | **2.500**（5 秒窗口位移 `x 102.54→90.03`） | **0.000** |
| **敌人速度·乌龟（走路）** | **2.5 格/秒**（出处：`Prefabs/Enemies/Green Koopa.prefab:166` 同上） | **2.500**（2 秒窗口 `x 85.99→80.98`；窗口不能更长 —— 1-1 那只 8 秒左右会掉进 x=72..74 的坑） | **0.000** |
| **敌人速度·龟壳（滑行）** | **7 格/秒**（出处：`Scripts/KoopaShell.cs:12` `rollSpeedX = 7`，`:50` 写进 `velocity`） | **6.738**（1.5 秒窗口滑行 10.13 格） | **-0.262（-3.7%）** —— 位移÷游戏时间量出来的偏差，来源是编辑器掉帧（`dt` 被夹在 0.05 上限）；容差 ±10%（判据在 `check_physics.py`） |
| **食人花出管周期** | 升 1.0 秒 / 停 **1.5** 秒 / 降 1.0 秒 / 停 **1.5** 秒（出处：`Prefabs/Enemies/{Green,Teal} Piranha.prefab:193` `absSpeed: 0.05`（单位/帧 ⇒ 3 格/秒）+ `:197/:198` `waitAtUpStop/waitAtDownStop = 1.5`；上下止点 `:86` `Up Stop local y=+2` / `:128` `Down Stop local y=−1` ⇒ 行程 3 格 ⇒ 1.0 秒） | 周期 **5.0 秒**、两段停 **各 1.5 秒**（`EnemyModule.Piranha` 的 `RiseTime 1.0 / HoldOut 1.5 / RiseTime 1.0 / HoldIn 1.5`；运行时 `[Enemy] 食人花完全伸出管口 → 开始缩回管里` 的间隔 1.5 秒、`缩回→伸出` 也 1.5 秒，见 `Probe.Piranha` 的窗口日志） | **0** |
| **1-2 升降台 上/下止点** | clone `Prefabs/Platforms/Moving Platform Vertical Spawner.prefab` 的 `Up Stop` local y = **+16.5** / `Down Stop` local y = **−6.5**（spawner 根与父容器 `Moving Platform Spawners` 的 y 都是 0） | 运行时组件读数（探针）：平台A `下止点=-6.5 上止点=16.5`、平台B 同值；`World1-2.txt` 两条实体行都带 `-6.5 16.5` | **0** |
| **1-2 升降台 行程** | **23 格**（= 16.5 − (−6.5)） | 运行时读到的行程 **23.0 格**（`check_platform.py` 从 prefab 现解析后逐项比，退出码 0） | **0** |
| **1-2 升降台 速度** | `Moving Platform Vertical.prefab:172` `absSpeed: 0.05`（单位/帧）× 60fps = **3 格/秒** | 运行时 **3.00 格/秒**（组件读数；逐帧采样 3.01/2.99/3.01/3.00） | **0** |
| **1-2 升降台 止点停留** | 同 prefab `:175`/`:176` `waitAtUpStop` / `waitAtDownStop` = **0** | 运行时 **0.0 秒** | **0** |
| **1-2 升降台 台面尺寸**（E-21② 已复刻） | `Moving Platform Vertical.prefab:121`（BoxCollider2D）与 `:227`（SpriteRenderer）`m_Size: {x: 3, y: 0.5}` = **3 格宽 × 半格厚** | **3.00 × 0.50 格**（探针读台面 `SpriteRenderer.sprite.bounds.size`；另由像素复核：一帧里量得 **3.00 × 0.43~0.45 格**，见 `Screenshots/contact-platform.png` 格 01/02） | **0**（像素量的 0.43~0.45 是 48×8 素材上下各 1 px 透明边的量化结果） |
| **1-2 升降台 x 落点**（E-21③ 已复刻） | clone 场景两个 spawner 实例的 root x = **152.8 / 137.8**（出处：`Scenes/World 1-2.unity` 的 PrefabInstance 覆写 `m_LocalPosition.x`） | **152.80 / 137.80**（探针读活对象 `transform.position.x`；`World1-2.txt` 的 `E 152.8 -4 …` / `E 137.8 14 …` 直接用原始 x，**不再落格心**） | **0**（旧实现按 `floor(x)+0.5` 落成 152.5/137.5，差 0.3 格） |
| **1-2 升降台 生成方式**（E-21① 已复刻） | spawner 每 **1.5 秒**生成一台（初值 = 间隔的一半 = 0.75 秒），且只在马里奥**水平 40 格**内生成；平台**离屏即销毁**（出处：`MovingPlatformVerticalSpawner.cs:14,23,39-47`、`:15`、`_common/DestroyOutOfScreen.cs:21-23`） | 运行时：`生成间隔中位数 1.502 秒`（两个 spawner 各 28 个间隔；逐个间隔 1.498~1.506，仅 1 个掉帧毛刺 1.78）、`生成距离 40 格`、销毁 50 次且每次 y∈{−2.25±0.04, 13.25±0.03}（= 画面带 [−2, 13] 之外 ⇒ 离屏） | **0** |
| **火球水平速度** | **20 格/秒**（出处：clone `Assets/Scripts/MarioFireball.cs:8` `absVelocity = new Vector2(20, 11)` 的 `.x` —— `:21` 初速与 `:26` 每帧重写都直接写进 `rigidbody.velocity` ⇒ 格/秒且**恒速**） | **20.000**（`Probe.Physics` 的 `MeasureFireball`：活对象位移 ÷ 游戏时间，104 个速度窗口的中位数；最小 0.000 = 撞墙那一瞬 / 最大 20.002） | **0.000** |
| **火球初速·竖直分量（向下）** | **−11 格/秒**（出处：同上 `:8` 的 `.y`，`:21` `velocity = (directionX*absVelocity.x, -absVelocity.y)`） | **−10.727**（下落段最小二乘外推到"按键那一帧"；另：首个速度窗口直接读数 **−12.619**，窗中心在出生后 0.035s） | **−0.273**（= 出生那一帧的重力台阶，口径同"起跳初速"那两行） |
| **火球撞地反弹速度（向上）** | **+11 格/秒**（出处：同上 `:50` `velocity = (velocity.x, +absVelocity.y)`；撞顶 `:52` 取 `-absVelocity.y`） | **+10.668**（上升段最小二乘外推到撞地帧） | **−0.332**（同上，一帧重力台阶） |
| **火球重力** | **53.96 格/秒²**（出处：clone `Assets/Prefabs/Player/Mario Fireball.prefab:64` `m_GravityScale: 5.5` × `ProjectSettings/Physics2DSettings.asset:7` `y=-9.81`；⚠️ 与马里奥的 5.3 **是不同预制体**） | **53.93 / 53.94**（下落段 / 上升段各自拟合；交叉印证：反弹后峰值高 1.055 格 ⇒ 11²/(2×1.055) = 57.4） | **0.03 / 0.02**（在采样误差内） |
| **火球联合印证**（自证） | — | 落差 12.13 格 + 初速 −10.73 + 重力 53.93 + 落速上限 24 ⇒ 推出落地 **t=0.573s**，实测撞地 **t=0.571s** | **0.002s** |
| **火球出生点** | clone `Mario.cs:247` `Instantiate(Fireball, FirePos.position, …)`；`FirePos` 是 Mario 的子节点、local `(0.5, 1)`（出处 `Prefabs/_managers/Level Starter.prefab:749`） | 我方 `feet + (±0.4, +0.6)`（`Fireball.Init`） | ⚠️ **本轮未搬**：clone 的 Mario **根节点原点约定（脚底 or 中心）没确认**，硬套会整体偏约 1 格 ⇒ 登记「允许的差异」E-22，**不硬套** |
| **火球存活上限** | clone **没有**任何寿命字段（`MarioFireball.cs` 只在撞墙/撞敌人时 `Explode()`，也没有离屏销毁） | **4 秒**（`GameConst.FireballLife`），本项目为防泄漏所加 | 本项目新增 ⇒ 登记 E-22 |
| **升降台 出现点 / 初速方向** | 实例A（spawner @ x=152.8）`Spawn Pos y = −4`、`directionY = +1`；实例B（@ x=137.8）`Spawn Pos y = 14`、方向取 prefab 默认 **−1**（出处：`World 1-2.unity` 的实例覆写 + prefab `:144`） | `World1-2.txt`：`E 152.8 -4 MovingPlatform -6.5 16.5 1` / `E 137.8 14 MovingPlatform -6.5 16.5 -1`；运行时 `升降台 spawner 就位：x=152.80 出现点 y=-4.00 …初速方向 +1` / `x=137.80 出现点 y=14.00 …初速方向 -0` | **0** |
| **侧向进管的走距 / 换场止点**（原 **E-24**，2026-09-19 消除） | **"撞到管口面才换场"，走距不是常量**：clone `Scripts/PipeWarpSide.cs:29` 触发时 `mario.AutomaticWalk(levelEntryWalkSpeedX)` 自动往右走，`:37` 的 `OnCollisionEnter2D` —— **撞到管口面那道实体挡墙**（`Prefabs/Pipes/Warp Green Pipe Side Short.prefab` 的 `Portal`，`BoxCollider2D offset(-2,0) size(0.2,2)`）才 `LoadNewLevel` | 走距 = **"从当前右边缘到管口面"**（`PlayerActor.StartPipeEnterSide`：`distance = faceX − Bounds.xMax`，管口面取自关卡数据 `# side-exit` 或密室 `PipeWarpTable.BonusRoomExitFaceX` —— 两处都不在代码里写数）。运行时：`[Player] 侧向进管：管口面 x=163.00（关卡数据 # side-exit）、当前右边缘 x=162.95 ⇒ 走距 0.05 格（走到管口面为止，不多走）`；末点 `管中移动结束：pos=(162.63,3.00)` ⇒ 右边缘 = 162.63 + 0.375 = **163.00 = 管口面**（不再越面） | **0**（旧实现走"自身贴图宽度"1.00 格 ⇒ 末点 = 管口面 + 0.58、进实心格 0.87 格；判据 `section12` 的 `管口前逐采样` 现在 `身处实心格命中 0 次`） |
| **1-2 主关出生点**（原「写不出出处」，2026-09-19 订正，见验收表 E-26） | clone `Scenes/World 1-2.unity` 的 `Spawn Points` 容器第 0 个子物体 `Spawn Point` = **(0, 9)**（出处三段见 E-26：`&887453260` 的 local/world + `GameStateManager.cs:32 spawnPointIdx=0` + `LevelManager.cs:568 GetChild`；地形上由原版整关海报 `row 17` 的 `x=−2..2` 天花板缺口印证） | `client/Assets/Resources/Levels/World1-2.txt` 的 `# spawn 0 9` ⇒ 运行时 `17:40:25.641 马里奥已就位：pos=(0.0, 9.0)`（探针 `spawnsafety`）；改前是无出处的兜底 `MinTileX+3.5` = (0.5,0) | **0** |
| **1-2 出生点 ↔ 最近两只栗宝宝的距离** | **13 / 14 格**（栗宝宝 `E 13 0` / `E 14 1`；这两格与原版海报 row 27 col 16、row 26 col 17 处那两处精灵**逐格相同**，一个数没动） | **13 / 14 格**（同一份数据；**本轮修掉"入场卡期间世界照跑"后**，`→ Stage` 那一刻它们仍在起点 `x=13.48 / 14.48`（2026-09-19 18:04 实测，修前是 `8.32 / 9.32`）⇒ 距离就是原版的 13/14 格） | **0** |
| **1-2 起点安全的"玩家可控时长"** | **≈4.9 秒**（原版敌人与马里奥**同时**开始：原版那张入场卡是**另一个场景**，clone `Scripts/LevelManager.cs:407,433 LoadSceneDelay("Level Start Screen", …)` ⇒ 卡期间世界不跑） | **5.07 秒**（2026-09-19 18:04 `spawnsafety` 实测：`→ Stage` → `马里奥死亡` = 5.07 秒 / 出生点口径 7.12 秒。修法 = `AppFlow.EnterLoading` 在入场卡期间把 `timeScale` 冻成 0、`ExitLoading` 解冻 ⇒ 卡那 2 秒里实体 `Update()` 的 `dt`=0（`EnemyModule.cs:179`），人与敌人**同时**起步，与原版同形） | **0**（原版 ≈4.9 vs 我们 5.07；差 0.17 秒 = 半格内的碰撞余量。修前是 3.05 秒 / 差 −1.8 秒，见「已处置的取证缺陷」⑧） |

---

> **说明（2026-09-19 交付收口）**：第四节**每一行都带出处**、且都有**运行时读数**（`check_physics.py` / `check_platform.py` 退出码 0）。
> **全表差值的收口口径**：每行要么 **差值 = 0**，要么就在「差值」列里写明**为什么不是缺陷**。四条"非 0 且超容差"的行逐条登记：
> ① `1-1 实心格数` **+44**（clone 把方块实例也展开成实心格，我们记成实体）；② `1-2 瓦片总数` **+15**
> （clone 按 prefab 默认尺寸**粗展开**；权威判据是逐格比对：`check_sections_12.py` **FAIL=0** / `verify12_full.py` **826 == 826**）；
> ③ `1-1 地面连通段` 右端 **1 列 2 格**（clone 的 7 格宽地面块外溢，而海报与 tilemap 都止于 x=209 ⇒ 不补）；
> ④ `管口/拐角 8 张瓦片 vs 原版海报` 的 **48~138/256** 残差（**已解释、不是缺陷**：同一批瓦片与**权威载体 clone** 逐像素差 0，
> 残差来自海报这个**参考图**载体；⛔ 不照海报改瓦片。判据 `python 已清理（一次性产物）` = 类别图 PASS）。
> 其余非 0 的差值都在**容差 / 采样误差**内（起跳初速 −0.23、跑跳 −0.27、满按峰值 −0.03、龟壳速度 −0.262（掉帧）、
> 火球 6 行里的 0.02~0.33），或已按 **E-22** 登记（`最大下落速度` / `火球出生点` / `火球寿命` / HUD 的**字号占屏比**）。
> `火球出生点` 那行是**明确未搬**（理由与何时消除见 E-22），不是"没找到出处"。
> 第三节的 HUD 列位置是"设计基准 = 原版归一化区间"，**像素级复核标 HUMAN-ONLY**（16:9 vs 4:3 的画幅差异，登记 E-22）。
