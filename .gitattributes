# git-lfs：只有"用户明确要求把原版素材随交付一起给"时才启用（skill §1.9-9 / asset-sources.md §8.2）
# 原版资源目录本身仍留在 .gitignore 里；提交的是仓库根的 原版资源.zip。
*.zip filter=lfs diff=lfs merge=lfs -text

# 引擎/Unity 二进制：按二进制处理，免得 git 做行尾转换、diff 刷屏
*.png  binary
*.jpg  binary
*.wav  binary
*.mp3  binary
*.ogg  binary
*.unity binary
*.prefab binary
*.asset binary
*.controller binary
*.anim binary
