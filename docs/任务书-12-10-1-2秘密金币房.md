# 任务书 —— 12-10：1-2 的进管传送 + 秘密金币房（本工程唯一未实现项）

> 主 agent 出（skill §5：主 agent 定契约 / 写步骤文档 / 派活 / 验收，不做实现）。
> 执行者是子 agent；**任务书里没写的，不许自己加、不许自己改契约**。
> 目标一句话：把 1-2 的"进管 → 秘密金币房 → 出管回主关"做到 **1-1 金币房同等的完整度**。

## 0. 参考物与判据（skill §0.5：写不出出处的量不许进代码）

- **A**：Super Mario Bros.（NES,1985）的 **World 1-2 秘密金币房**。
- **权威载体**：`原版资源/参考工程/SMB-clone/Assets/Scenes/World 1-2 - Underground.unity`（场景数据）。
  已解析结论：`原版资源/解析/原版1-1与1-2元素表.md` **§4「1-2 密室」**（先读它，再决定要不要重跑解析脚本）。
- **坐标标定**：`T = clone world − (9,3)`（元素表 §0.1）。
- **现成模板**：`client/Assets/Resources/Levels/World1-1-Underground.txt`（1-1 密室，已跑通）——
  文件头有格式与出处写法，**照它的形状写**，⛔ 别自己发明格式。

## 1. 交付物（缺一不可）

| # | 产出 | 路径 |
|---|---|---|
| 1 | 1-2 密室关卡数据 | `client/Assets/Resources/Levels/World1-2-Underground.txt`（格式同 `World1-1-Underground.txt`：`T <x> <y> <layer> <sprite>` / `E <x> <y> <prefab>`） |
| 2 | 进管 / 出管接线 | 1-2 主关里那根进管 → 密室；密室出口管 → 回 1-2 主关对应位置（**接线点先读 1-1 的现成实现**，照着接，见 §2） |
| 3 | 实机证据 | 探针场景 `Probe.CoinRoom12` + 5 张截图（见 §3） |
| 4 | 回报 | 见 §5 格式 |

## 2. 必须复用的现成实现（⛔ 不许另写一套）

1-1 金币房已经跑通，接线与判定都在工程里，**先读再抄**：

| 关注点 | 去哪读 |
|---|---|
| 进管/出管的状态编排（含"管中移动"、冻结主关、密室加载） | `client/Assets/Scripts/Module/Flow/AppFlow.cs`：`EnterBonusRoom` / `BeginBonusRoom` / `ExitBonusRoom` / `FinishExitBonusRoom` / `DiscardBonusRoom` |
| 密室上下文（子区域/地下/落点覆盖） | `StageContext.SubArea` / `Underground` / `SpawnOverride`（同目录） |
| 站管口按 ↓ 的判定、管中移动速度 | `client/Assets/Scripts/Module/Player/PlayerActor.cs`（`PipeMove`）、`Core/GameConst.cs` |
| 关卡数据怎么解析成瓦片/实体 | `client/Assets/Scripts/Module/Level/LevelModule.cs`（`Resources/Levels/*.txt` 的运行期解析） |
| 1-1 的进/出管参数从哪来 | grep `World1-1-Underground` 与 `IsSolidTile`/`Pipe` 相关常量 |

**硬约束**：
- ⛔ 不许改公开签名；⛔ 不许新增"另一套"进管机制 —— 1-2 必须走**同一条** `EnterBonusRoom` 链路；
- ⛔ 不许编数值：进管触发格、出管落点、房间瓦片/金币数量全部要有出处（clone 场景 `文件:行` 或元素表 §4）；
- 时间在密室里**不停**（与 1-1 一致，实测 1-1 是 371→363）；走出密室后回 1-2 主关**同一位置**。

## 3. 实机证据（判据是我验收时逐条对的）

探针文件：`tools/probes/probe.cs`，**新增** `public static void CoinRoom12() => Start("coinroom12", CoinRoom12Body);`
（写法照现成的 `CoinRoomBody`，它已经跑通 1-1 那条链路）。

必须产出这 5 张图（名字逐字一致，验收表要引用）：

| 截图名 | 拍什么 |
|---|---|
| `26-pipe12-stand.png` | 站在 1-2 那根进管管口（进管前） |
| `27-coinroom12-enter.png` | 密室内（进管后，刚落到房间） |
| `28-coinroom12-floor.png` | 密室地面/金币区（吃到币之后） |
| `29-coinroom12-exit.png` | 出管瞬间/刚回主关 |
| `30-back-in-12.png` | 回 1-2 主关稳定后 |

日志必须能读出这些行（沿用现有 tag，别改格式）：
`[Pipe] 进管：…`、`[Level] 关卡已解析 World1-2-Underground：瓦片 N、实体 M、范围 …`、金币数变化、
`[Pipe] 出管完成：玩家出现在出场管顶 (x, y)`、以及场景自己的 **`场景结束：coinroom12`** 标记。

## 4. 采样链路纪律（skill §1.13 第 5 条，实测踩过很多次）

1. **跑之前先自检**（秒级）：`.ai-tmp` 下的 `.ps1` **语法 0 错 + 无 BOM 不许含非 ASCII**；
2. 完成判据**只能是**探针自己写的 `场景结束：<name>` 标记，且按"**新增次数**"判；**超时只当兜底**；
3. 读游戏日志要 `FileShare.ReadWrite`；日志路径按**当天**取（`client/Logs/2026-09-18.log`）；
4. 编译必须成功（`unity command recompile_status` → `compilationFailed:false`）才谈实测；
5. 跑完把日志里的 **Error / NullReference 清零**（修掉或回报里给出处）。

## 5. 回报格式（子 agent 用）

```
产出物：<绝对路径清单>
自检：<实际执行的命令 + 原始输出（含 run.ps1 的 PREFLIGHT/SCENE_DONE 行）>
未决：无 / <具体条目>
```

**⛔ 不许**产出 `docs/交接-*.md` / `NEXT.md` / `docs/进度*.md`（skill §1.5 第 8 条）。

## 6. ⛔ 文件所有权（写别人的文件 = 冲突）

| 只许写 | 不许写（要改就写进回报，由主 agent 改） |
|---|---|
| `client/Assets/Scripts/**`、`client/Assets/Resources/Levels/**`、`client/Assets/Screenshots/**`、`.ai-tmp/test/**`、`原版资源/**`（只许追加解析产物与脚本） | `策划/**`（验收表/对照表/规格）、`docs/**`、`tools/**`、**任何 skill**（全局源 `clover-ai-skill/`、宿主安装副本都不许动） |
